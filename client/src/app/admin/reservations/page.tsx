// TODO: Admin reservations management page
// - GET /reservations?status=pending
// - PATCH /reservations/:id/status  → confirm / cancel
export default function AdminReservationsPage() {
  return <div>Admin Reservations — Coming Soon</div>;
}
