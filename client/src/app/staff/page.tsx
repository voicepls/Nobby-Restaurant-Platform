// TODO: Live Request Dashboard for staff
// - Real-time WebSocket feed (Socket.IO)
// - Shows all incoming orders + service calls
// - Staff-only (protected route)
export default function StaffPage() {
  return <div>Staff Dashboard — Coming Soon</div>;
}
