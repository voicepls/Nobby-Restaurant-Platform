// Shared utility functions
// TODO: formatPrice(pkr: number): string  →  "Rs. 2,800"
// TODO: formatDateTime(iso: string): string
// TODO: cn(...classes: string[]): string  (classnames helper)
export {};
