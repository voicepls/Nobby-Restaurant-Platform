// TODO: Service call buttons — Waiter / Hostess / Cleaner
// Sends POST to /service-calls → triggers WebSocket event to staff dashboard
export function ServiceCallButtons({ tableId }: { tableId: number }) {
  return <div>Service Call Buttons — TODO</div>;
}
